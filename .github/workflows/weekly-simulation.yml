name: Weekly Simulation
on:
  schedule:
    - cron: '0 3 * * 0'
  workflow_dispatch:
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Run all simulations
        run: curl -s -X POST https://tensorworks.online/api/simulate -H 'Content-Type: application/json' -d '{}'
