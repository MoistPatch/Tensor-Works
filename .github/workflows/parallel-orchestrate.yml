name: Daily Parallel Orchestration

on:
  schedule:
    - cron: '30 1 * * *'
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Run parallel agent pipeline
        run: |
          SITE="${{ vars.SITE_URL || 'https://tensorworks.online' }}"
          STATUS=$(curl -s -o /tmp/response.json -w "%{http_code}" \
            -X POST "${SITE}/api/parallel-orchestrate" \
            -H "Content-Type: application/json" \
            -d '{}')
          echo "HTTP ${STATUS}"
          cat /tmp/response.json
          if [ "$STATUS" -ge 500 ]; then
            echo "Parallel orchestration returned ${STATUS}"
            exit 1
          fi
